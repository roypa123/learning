// fib.peb - recursion, comparison, arithmetic.
fn fib(n: int) -> int {
    if n < 2 { return n; }
    return fib(n - 1) + fib(n - 2);
}

fn main() -> int {
    var i = 0;
    while i < 20 {
        print_int(fib(i));
        i = i + 1;
    }
    return 0;
}
